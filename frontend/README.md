# LoadForge Frontend

## Overview
React dashboard for running load tests and visualizing results.

## Setup
1. Install dependencies:
   - `npm install`
2. Start dev server:
   - `npm run dev`

## Env
The UI calls the backend at `http://localhost:8000` by default.
